明白，ZEE。
你说 **继续** → 我马上执行，不问、不等、不中断，直接开始 **Phase 11 — SME 360° Business Loan Dashboard（企业融资画像总览）** 的正式交付。

下面是 **可立即复制给 Replit 执行的完整指令包**。
你不需要思考任何东西，只需贴给 Replit，它就会自动构建所有文件、路由、API、前端界面。

---

# 🚀 **PHASE 11 — SME 360° DASHBOARD（供 REPLIT 直接执行）**

# ===============================================

请严格依照顺序执行，不要跳过任何一条。

---

# ============================================================

# ✅ 【任务 1】创建 SME 360° 主页面模板

# ============================================================

创建文件：

```
templates/sme_360.html
```

内容要求（Replit 请直接写出完整 HTML）：

### 页面结构（Galaxy Theme，现代卡片式界面）

1. **Business Credit Profile（企业风险画像）面板**

   * BRR 等级（1–10）
   * Approval Odds（以圆形进度条显示）
   * DSCR（Debt Service Coverage Ratio）
   * DSR（Cashflow DSR）
   * Cashflow Variance（波动系数）
   * CTOS SME Score
   * Industry Risk（BNM行业）

2. **SME Cashflow Insights（现金流洞察）图表区域**

   * 12 个月营收折线图
   * 12 个月支出折线图
   * Net Cashflow Area Chart
   * AR Aging / AP Aging（柱状图）

3. **SME Loan Capabilities（企业融资能力）**

   * Maximum Loan Amount（基于 BRR + DSCR）
   * Maximum EMI
   * CGC Eligibility（是否符合担保资格）
   * SME Loan Simulator（贷款模拟器字段）

     * Loan Amount
     * Tenure（月）
     * 按钮：Calculate

4. **SME Loan Product Recommendations（企业贷款产品推荐）**

   * Funding Societies
   * Aspirasi
   * CapitalBay
   * SME Bank / Maybank SME
   * 匹配原因（Match Score + Explanation）

---

# ============================================================

# ✅ 【任务 2】创建 SME 360° CSS 样式

# ============================================================

创建文件：

```
static/css/sme_360.css
```

要求：

* Galaxy Theme（黑色背景、粉色主色、紫色边框高亮）

* 四面板采用栅格布局（grid-template-areas）

* 卡片样式与 customer_360.css 保持一致

* 风险等级样式：

  * BRR 1–2 → 绿色
  * BRR 3–4 → 黄色
  * BRR 5–6 → 橙色
  * BRR 7–10 → 红色

* Approval Odds 采用 SVG 或 CSS 动画的环形进度条

---

# ============================================================

# ✅ 【任务 3】创建 SME 360° JS 引擎

# ============================================================

创建文件：

```
static/js/sme_360.js
```

JS 负责：

### （1）从后端拉取 SME 数据：

```
GET /api/sme/<company_id>/loan-profile
```

### （2）渲染：

* BRR Badge
* Approval Odds 圆圈
* DSCR / DSR / Variance
* CTOS SME Score
* 行业风险文字 + 颜色编码
* 图表（Chart.js）
* 推荐产品卡片

### （3）SME Loan Simulator：

输入：

* Loan Amount
* Tenure

JS 执行：

* EMI = (P × r × (1+r)^n) / ((1+r)^n - 1)
* 新 DSCR 计算
* 新 Approval Odds 映射
* 新 BRR 调整（若需要）

---

# ============================================================

# ✅ 【任务 4】新增 FastAPI 后端（SME 数据整合端点）

# ============================================================

创建：

```
accounting_app/routes/sme_360.py
```

新增端点：

```
GET /api/sme/{company_id}/loan-profile
```

该端点需整合：

### 数据来源：

* Phase 2 的 **SME 风控引擎（BRR/DSCR/Variance/Industry Risk）**
* Phase 3 的 **自动解析器（Bank Statements + Cashflow Variance）**
* suppliers_invoices：应收/应付账领（AR/AP）
* income_documents：营业额来源
* business_loans.py Modern Engine（计算 loan capability）
* loan_products_catalog：SME 产品推荐算法

### 输出格式（Replit请严格遵守）：

```
{
  "company_id": "...",
  "company_name": "...",
  "brr_grade": 3,
  "approval_odds": 82,
  "dscr": 1.85,
  "dsr": 1.32,
  "cashflow_variance": 0.22,
  "ctos_sme_score": 710,
  "industry_risk": "Medium",
  "monthly_revenue": [...],
  "monthly_expenses": [...],
  "ar_aging": {...},
  "ap_aging": {...},
  "max_loan_amount": 350000,
  "max_emi": 8200,
  "cgc_eligibility": true,
  "recommended_products": [...],
  "advisor": "AI生成企业贷款建议"
}
```

---

# ============================================================

# ✅ 【任务 5】Flask 前端路由

# ============================================================

在 app.py 添加：

```
@app.route('/sme-360/<company_id>')
@require_admin_or_accountant
def sme_360(company_id):
    return render_template("sme_360.html", company_id=company_id)
```

---

# ============================================================

# 完成后请回复：

# ============================================================

```
PHASE 11 SME 360 DASHBOARD COMPLETE
```

---

# 📌 文档更新（replit.md）

请更新：

* Phase 11 全部新增文件
* SME 360 数据流流程图
* API结构
* 新前端页面入口：

  ```
  /sme-360/<company_id>
  ```

---

# ✅ 任务包结束

ZEE，你现在无需做任何决定，
只需把上面这一整份 **Phase 11 指令包** 复制 → 贴给 Replit。

我随时准备继续进入下一阶段（Phase 12）。
