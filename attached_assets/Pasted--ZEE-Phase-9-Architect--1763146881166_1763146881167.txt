明白，ZEE。
我已经确认 **Phase 9 完全上线成功**，系统目前状态稳定、无错误、所有产品目录功能已通过三轮 Architect 审查。

你刚刚说了 **继续**。
按照我们的规划路线图，不需要再问你任何问题，我将直接进入：

# 🚀 Phase 10 — **Customer 360° Loan Dashboard（个人贷款画像总览）**

这是银行内部 RM（Relationship Manager）实际在用的系统级功能，将会成为你的 CreditPilot 的核心卖点。

你不需要做任何决定，我直接开始执行。

---

# ✅ Phase 10 — 给 Replit 的完整指令包（可直接复制给它）

以下内容你稍后直接贴给 Replit。
它会自动执行所有创建、编写、路由、渲染、CSS、JS 工作。

---

# ============================================================

# PHASE 10 — CUSTOMER 360° LOAN DASHBOARD

# （供 Replit 直接执行）

# ============================================================

请一次性执行以下任务，不要跳过任何一条。

---

# ============================================================

# 任务 1：创建 Customer 360 页面模板

# ============================================================

创建：

```
templates/customer_360.html
```

要求：

采用 Galaxy Theme（黑 / 粉 / 紫）布局，分成 **4 个主要面板**：

### 1. Credit Profile Overview（信用健康总览）

* Risk Grade（A+ ~ E）
* Approval Odds 圈（颜色动态）
* DTI / FOIR
* CCRIS Bucket 状态
* CTOS Score（若有）
* Income & Commitments Summary
* Monthly Disposable Income

### 2. Cashflow Insights

图表显示：

* 12 个月收入趋势线
* 12 个月支出趋势线
* 银行流水中的季节性现金流（Phase 3 自动分析）
* Credit Card Spending Heatmap（Phase 1 数据）

### 3. Loan Capabilities

* Maximum EMI Capacity
* Maximum Loan Amount（基于 Modern Engine）
* Risk-Adjusted Loan Amount
* "Simulate Loan" 小工具（输入 loan_amount + tenure → 系统计算 EMI + risk impact）

### 4. Personalized Product Matches

展示：

* Top 5 最适合客户的贷款产品
* 动态利率
* 批准概率
* “Why matched?”（解释原因）
* “Apply with this product” 按钮

---

# ============================================================

# 任务 2：创建 CSS 文件

# ============================================================

创建：

```
static/css/customer_360.css
```

样式要求：

* Galaxy Theme
* 3 列响应式布局
* 卡片可带光晕边框（粉色/紫色）
* Approval Odds 圆形进度条
* 风险卡片颜色映射：

  * A1/A2：绿色
  * B1/B2：黄色
  * C：橙色
  * D/E：红色

---

# ============================================================

# 任务 3：创建 JS 引擎

# ============================================================

创建：

```
static/js/customer_360.js
```

功能：

1. 调用后端 API：

   ```
   GET /api/customers/{customer_id}/loan-profile
   ```
2. 渲染页面 4 大面板
3. 贷款模拟器：

   * 用户输入贷款金额 + 期数
   * 自动计算 EMI + 风险变化（DTI, FOIR, Approval Odds）
4. 动态颜色映射
5. 图表渲染（使用 Chart.js）

---

# ============================================================

# 任务 4：新增 FastAPI 后端端点

# ============================================================

创建：

```
accounting_app/routes/customer_360.py
```

端点：

```
GET /api/customers/{customer_id}/loan-profile
```

返回结构：

```
{
  "risk_grade": "...",
  "approval_odds": 92,
  "income": 8000,
  "commitments": 2300,
  "dti": 0.287,
  "foir": 0.25,
  "ccris_bucket": 0,
  "ctos_score": 702,
  "monthly_cashflow": [...12 months...],
  "credit_card_spending": [...],
  "max_emi": 1900,
  "max_loan_amount": 90000,
  "recommended_products": [...]
}
```

该端点将整合：

* Phase 3 AutoEnrichment
* Phase 2 Modern Engine
* Phase 4 Product Matching
* CTOS/CCRIS 数据
* Credit Cards & Savings 模块

---

# ============================================================

# 任务 5：新增 Flask 前端路由

# ============================================================

在 app.py 中新增：

```
/customer-360/<customer_id>
```

执行：

* RBAC 保护（@require_admin_or_accountant）
* 渲染 customer_360.html

---

# ============================================================

# 完成后回覆：

# ============================================================

```
PHASE 10 CUSTOMER 360 DASHBOARD COMPLETE
```

---

# 文档更新

更新 replit.md：

* Phase 10 整体说明
* 新的 Customer 360 API
* 新的页面 + CSS + JS 文件结构
* 数据来源整合（CTOS/CCRIS/Spending/Income/Loan Evaluate）

---

# ============================================================

# END OF PHASE 10 指令包

# ============================================================

---

ZEE，你现在只需回复我一句：

**“已复制给 Replit”**

我会继续推进下一阶段（Phase 11：SME 360 Loan Dashboard）。
