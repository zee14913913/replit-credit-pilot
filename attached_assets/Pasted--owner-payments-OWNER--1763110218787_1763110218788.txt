好，这下我们把线拉直，一次讲清楚，不再乱用这些字段。

⸻

先确认你的定义（以你为标准）

你刚刚给的是最终权威定义：
	•	owner_payments
= OWNER 客户自己付信用卡的付款金额分类明细（你客户自己的还款）
	•	gz_payments
= INFINITE / GZ 帮客户代还信用卡的金额分类明细（你们公司帮还）

再加你这句是关键：

不要把信用卡部分用于其他页面的分析里面
commitment 我们用 CTOS report 来判断（和银行一致）

也就是说：
	1.	这两个字段只属于「信用卡管理业务」模块，用来算：
	•	每张卡还了多少
	•	客户 vs 你们帮他还的比例
	•	卡行为分析（lazy还款、代还比例、风险之类）
	2.	不能再用于：
	•	DSR / DSRC / DSCR 的收入
	•	DSR / DSRC / DSCR 的债务 commitment
	•	任何「个人 / 企业贷款评估」模块
	3.	贷款 commitment 的唯一正确来源：
👉 CTOS 报告（个人/公司） + 你上传/解析后的结构化数据
这才跟银行实际审批逻辑对齐。

你说得非常清楚，而且你是对的。
之前我让 Replit 在贷款模块里用 owner_payments + gz_payments 来推「月债务」这件事，从「银行标准」角度看确实是不应该。

⸻

接下来要做的事：把“信用卡字段”从贷款线里全部拔掉

为了跟你的业务定义对齐，我们要做两件事：

1️⃣ 规则重置：这些字段的使用范围

允许使用的范围：
	•	仅限：信用卡 / statement / card dashboard 相关的：
	•	卡账单分析
	•	代还服务分析（你们帮客户还多少钱）
	•	现金流图、卡风险图、Owner vs GZ 比例

禁止使用的模块：

下面这些都不允许再用 owner_payments / gz_payments / closing_balance_total 来算「贷款 commitment」：
	•	个人：
	•	LoanEligibilityEngine（贷款资格）
	•	LoanAffordabilityEngine（贷款承受能力 E）
	•	LoanProductEngine（Phase C 产品模拟）
	•	企业：
	•	BusinessIncomeEngine
	•	BusinessLoanEngine（DSCR）
	•	BusinessLoanProductEngine
	•	双数据源 DSCR（journal / monthly）

这些模块的“债务 / commitment”，必须改成走 CTOS 线（或者暂时缺省，等待 CTOS 引擎）。

⸻

2️⃣ 给 Replit 的指示（你可以整段复制过去）

这个是给 Replit 的“技术更正指令”，我写成可直接贴过去的版本：

⸻

发给 Replit：

重要业务更正（请务必按以下要求调整）：

1）字段业务定义（最终版）
	•	monthly_statements.owner_payments：客户自己支付信用卡的还款金额（信用卡模块内部使用）
	•	monthly_statements.gz_payments：INFINITE/GZ 帮客户代还信用卡的金额（信用卡模块内部使用）

这些字段只用于：信用卡账单管理 / 代还分析 / 卡行为分析。

👉 禁止在任何「个人/企业贷款、DSR、DSRC、DSCR、贷款承受能力、贷款产品模拟」中，用这两个字段来推导“收入”或“贷款 commitment”。

⸻

2）需要从贷款模块里移除的逻辑
请在以下模块中，移除对 owner_payments / gz_payments / closing_balance_total 作为“债务来源 / 收入来源”的使用：
	•	accounting_app/services/loan_eligibility_engine.py
	•	accounting_app/routes/loans.py
	•	accounting_app/services/loan_affordability_engine.py
	•	accounting_app/routes/loan_affordability.py
	•	accounting_app/services/loan_product_engine.py
	•	accounting_app/routes/loan_products.py
	•	accounting_app/services/business_loan_engine.py
	•	accounting_app/routes/business_loans.py（包括 journal / monthly 双模式）
	•	accounting_app/services/business_loan_product_engine.py
	•	accounting_app/routes/business_loan_products.py

具体调整要求：

A. 个人贷款（DSR / Affordability / Products）
	•	不要再从 monthly_statements 自动计算 monthly_debt：
	•	不再使用 owner_payments + gz_payments + closing_balance_total * 0.05
	•	LoanEligibilityEngine 和 LoanAffordabilityEngine：
	•	短期做法：
	•	要么改为必须从 API 参数接收 monthly_debt（前端通过 CTOS 结果填入），
	•	要么在没有 commitment 数据时，明确返回状态：
	•	"status": "missing_commitment_data"
	•	不要给出 DSR 数值，以免误导。

B. 企业贷款（BusinessLoanEngine / DSCR / Products）
	•	annual_debt_service 不再从信用卡还款推导。
	•	不再使用 owner_payments + gz_payments + closing_balance_total * 0.05 * 12。
	•	BusinessLoanEngine 中：
	•	operating_income 继续来自 journal / business income / IncomeService（按照之前确定的优先级）。
	•	annual_debt_service 将来要接入 CTOS / 企业贷款表，当前阶段如果没有真实数据：
	•	要么通过 API 参数显式传入（例如 annual_debt），
	•	要么返回状态 "status": "missing_commitment_data"，不强行计算 DSCR。
	•	dual-source DSCR（source=journal / source=monthly）：
	•	两个模式的差异只允许体现在“收入来源 / 会计 vs 月结单视角”或将来扩展的配置上，
	•	不允许通过信用卡还款字段构造“年债务服务”。

⸻

3）信用卡模块的字段使用范围（请保留现状）
	•	在「信用卡管理 / monthly_statements / credit_cards / 卡分析」相关代码中，
继续保留 owner_payments / gz_payments 的逻辑，用于：
	•	区分客户自己还 vs 我们帮他还
	•	计算卡还款行为、风险、代还比例等
	•	但这些结果只在信用卡 / Statement / 现金流可视化使用，
不得再作为「贷款评估的 commitment / income」输入。

⸻

4）后续准备（先不用实现，只是预留）
	•	后续会新增一个 CTOSCommitmentEngine（个人 + 企业），从 CTOS 报告解析真正的贷款 commitment：
	•	个人：housing loan / personal loan / hire purchase / credit cards / overdraft 等
	•	企业：term loan / OD / trade line / corporate cards 等
	•	贷款相关模块（DSR / DSCR / Affordability / Loan Products）之后应全部改为只消费 CTOS 引擎的结果，不再碰信用卡字段。

当前这一步的目标非常简单：
👉 把信用卡字段 owner_payments / gz_payments 完全从贷款评估逻辑里面移除，只保留在信用卡业务模块中使用。

谢谢。





⸻

总结一下现在的“原则版本”
	•	✅ 信用卡字段 = 只在信用卡业务里玩
	•	✅ 贷款承受能力 / DSR / DSRC / DSCR = 只看「收入模块 + CTOS commitment」
	•	✅ 在 CTOS 模块接上之前，宁愿标记“缺少 commitment 数据”，也不要乱猜
	•	✅ 目标是对齐「银行真实评估方式」，不是系统自己乱估算

接下来等 Replit 按这套指示把“owner/gz payments 逻辑”从贷款线拆出去，我们再一起设计下一步：
CTOS 报告 → Commitment 引擎 → 贷款模块统一用 CTOS 数据。这样整条链就完全跟马来西亚银行的真实流程对齐了。