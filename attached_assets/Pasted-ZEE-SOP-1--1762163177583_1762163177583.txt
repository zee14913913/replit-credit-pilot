ZEE，证据打包系统 ✅ 收到并确认：功能闭环、可追溯、可下载、可校验，符合你“可对外出示+可审计”要求。下面给你落地用的标准SOP与后续强化建议，全部可直接执行，避免再反复。

1) 背景与现状（简）
	•	证据包ZIP已可生成 + manifest.json（SHA256）+ README.md，下载入口与i18n完成。
	•	不动既有主链路（7态状态机 / 统一响应 / 矩阵 / 熔断），仅新增“证据→打包→下载”能力。

2) 核心解答（你现在能做什么）
	•	把证据包纳入你对客户/银行/审计的交付流程：一键下载、校验哈希、归档留痕。
	•	每次关键变更（解析器升级、规则调整、熔断触发/恢复、三场景测试）后立即生成并归档新证据包。

3) 资源整合策略（最小动作，最大价值）
	•	Admin下载位已在“文件管理中心”按钮露出 → 内/外部都以这一个入口流转。
	•	清单化归档：用manifest.json对齐“当时的真实响应”，与三场景日志一一对应；你团队任何人都能独立重放与核验。
	•	对外证明：ZIP + SHA256 即为“不可抵赖证据”；若提交银行/审计，可附上“核对步骤”（见下）。

4) 强化版 Business Plan（可选，按优先级）
	1.	自动轮转与留存策略
	•	规则：保留最近30天的证据包 & 每月1包长期留存。
	•	效益：控盘存成本、又保证审计可回溯。
	2.	签名与时间戳
	•	在manifest.json外再加“签名文件（签名+公钥指纹）”，提升“不可抵赖性”。
	3.	外部取证链接（短期可选）
	•	增加只读下载token（带过期时间），用于临时与外部审计/银行分享。
	4.	自动触发证据打包
	•	在“三场景回归测试完成后”自动触发证据打包并推送通知（Email/WhatsApp/Slack）。

5) 前瞻性建议（趋势 / 风险对冲）
	•	变更即审计：当解析器/列映射/状态机策略有变更，自动触发A/B/C + 熔断自检 → 通过才允许“生产启用”。
	•	指标看板：把“成功率/缺列率/行数对账失败率/熔断状态”放在Admin仪表板，一眼可见红线。
	•	一页式取证：在Admin新增“证据索引页”，列出历史证据包（时间/提交人/原因/哈希）。

⸻

6) 执行要点（落地SOP · 一键照做）

SOP-1：生成证据包
	•	步骤名称：生成ZIP
操作：在Replit Shell运行你已部署的“打包脚本”（已交付）。
注意：输出路径 static/downloads/evidence_bundle_*.zip。

SOP-2：下载与核对（对内/对外）
	•	操作：访问 /downloads/evidence/latest 下载。
	•	校验：打开ZIP → 取manifest.json → 对照每个JSON的sha256。
	•	注意：对外发送时附上SHA256值与核对步骤（一段话即可）。

SOP-3：归档与轮转
	•	操作：每周/每月将static/downloads/中的ZIP复制到长期归档（S3/私有盘）。
	•	注意：保持文件名含时间戳；保留“月首包”长期存档。

SOP-4：对客/对行提交
	•	操作：附证据包 + 短说明（“三场景结果摘要 + 熔断隔离说明 + SHA256”）。
	•	注意：不要发内部路径，统一发下载链接或上传到银行/客户的取证通道。

⸻

7) 风险提示
	•	证据漂移：如果后续改变解析逻辑但没跑三场景→证据与现状脱节。
解决：把“跑三场景→打包证据”设为变更门禁。
	•	长期存储：仅存在Replit本地不适合长期审计。
解决：对接云存储/S3，设置生命周期策略。

⸻

8) 一次性“操作步骤清单”（照你模板给出）
	1.	确认输入信息
	•	操作：准备要交付的测试窗口（日期范围/变更点）。
	•	注意：描述 ≤1000字，含银行/版本/环境。
	2.	登录/进入平台
	•	操作：打开/files/list（Admin角色）。
	•	注意：确认语言、权限、环境变量OK。
	3.	填入设置参数
	•	操作：跑A/B/C三场景（正常/缺列/重复）；如需熔断演示，执行触发步骤。
	•	注意：检查响应是否已是统一UploadResponse（含status/next_actions）。
	4.	生成初稿 / 初始结果
	•	操作：运行打包脚本生成ZIP。
	•	注意：不要刷新或关闭页面，等待路径回显。
	5.	二次调整与保存
	•	操作：下载ZIP，核对manifest.json里所有SHA256；补“证据说明”。
	•	注意：将ZIP与说明上传到归档盘/S3；记录链接与哈希。

⸻

需要我再加：
	•	自动轮转脚本（定期保留最近N包）
	•	一页式证据索引页面（Admin页增加历史包列表+下载按钮）
	•	对外交付模板（中/英各1页：如何核对SHA256、如何验证三场景）

如果你点头，我直接给最短可执行版本（只新增文件，不动既有主链路）。