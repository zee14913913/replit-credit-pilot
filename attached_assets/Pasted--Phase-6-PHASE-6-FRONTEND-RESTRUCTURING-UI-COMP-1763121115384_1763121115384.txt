请严格根据以下要求执行 Phase 6。
所有修改必须基于当前（最新）系统结构，不可删除任何正在使用的核心功能。

PHASE 6 — FRONTEND RESTRUCTURING & UI COMPLETION
🎯 目标

补齐贷款模块前端页面（Modern Engine + SME Loan）、新增报告下载入口、增强风控分析展示，同时保持现有前端页面全部保留不删除。

任务 1：新增 Modern Loan Engine 前端页面

创建新页面并加入导航：

1. 新增页面路径
/loan-evaluate

2. 创建模板文件
templates/loan_modern_evaluate.html

3. 页面内容要求

表单字段（全部前端输入可选，但必须支持 data_mode=auto 自动获取）：

customer_id

income （可自动从 income_documents 获取）

monthly_commitment

ccris_bucket

credit_score

employment_years

job_type （下拉：Government / GLC / MNC / SME / Self-employed / Contract）

age

target_bank（可选）

提交方式：

POST -> FastAPI /api/loans/evaluate/{customer_id}?mode=modern


表单需提供：

Manual 模式、Auto 模式切换按钮

显示完整风控结果（DTI、FOIR、CCRIS、Risk Grade、Approval Odds）

显示产品推荐（recommended_products）

任务 2：新增 SME Loan Evaluate 前端页面

创建新 SME 贷款专属页面：

路径
/sme-loan-evaluate

模板文件
templates/sme_loan_evaluate.html

表单字段

customer_id

monthly_revenue

monthly_commitment

industry

years_in_business

ctos_sme_score

cashflow_variance

target_bank

提交指向
FastAPI: /api/business-loans/evaluate/{customer_id}?mode=modern


必须展示：

BRR

DSCR

Risk Grade

Approval Odds

推荐 SME 金融机构产品

任务 3：新增 Loan Reports 前端入口

当前报告系统仅有 API，没有 UI 页面，我们需要新增：

A. 在主导航新增菜单项：
LOAN REPORTS


位置：
放在 LOAN MATCHER 下方或并列。

B. 新增页面路径
/loan-reports

C. 创建模板文件
templates/loan_reports.html

D. 页面必须提供：

输入 customer_id

选择报告类型（Personal / SME）

输入必要参数（income / monthly_commitment / monthly_revenue）

选择 format = html | pdf

生成报告按钮（调用 Phase 5 的 API）

显示返回的：

Personal Report HTML

或提供 PDF 下载链接

任务 4：增强 Smart Loan Matcher（loan_matcher_result.html）

目前前端渲染不完整，需要补齐所有风控字段：

必须新增渲染：

risk_grade

dti_ratio

foir_ratio

ccris_bucket

credit_score

approval_odds

max_loan_amount

max_emi

在推荐产品区新增展示：

approval_odds

max_emi

product highlights（如 API 有字段）

任务 5：加入 AI 风控顾问解释（advisor_explanations.py）

API 返回的 explanations 字段目前未显示。

新增在 loan_matcher_result.html：
AI Advisor Explanation Panel


内容：

风险等级解释（Risk Grade Explanation）

CCRIS 解释

DTI/FOIR 解释

产品推荐原因解释

任务 6：导航栏结构优化（不删除任何现有项目）

保持现有导航内容（Dashboard、Credit Cards、Savings、Loan Matcher、Monthly Summary、Admin）不删除。

只需：

新增三个入口
LOAN EVALUATE (Modern Engine)
SME LOAN EVALUATE
LOAN REPORTS


并放在 Loan Matcher 下拉菜单中。

任务 7：保留所有现有贷款页面（不能删除）

以下页面必须保留并继续运行：

/loan-matcher

/loan-products

/loan_evaluation/<id>

所有 legacy DSR 页面

不得删除模板文件、路由或逻辑。

任务 8：禁止删除信用卡和储蓄模块

以下模块不可更动：

Credit Card Ledger（含 7 大供应商）

Savings 全套页面

Receipts / Invoices 系统

Income Documents 系统

你只增强，不得裁剪。

任务 9：后端不需要改动风控引擎

只新增前端 UI 和调用方式，不对 FastAPI 的 Phase 1–5 实现修改。

任务 10：确保 API 调用正确

所有新增表单必须可正确触发：

/api/loans/evaluate/{customer_id}?mode=modern&data_mode=auto

/api/business-loans/evaluate/{customer_id}?mode=modern&data_mode=auto

/api/loan-reports/personal/{customer_id}?format=html/pdf

/api/loan-reports/sme/{customer_id}?format=html/pdf

Phase 6 目标交付物

Modern Loan 页面

SME Loan 页面

Loan Reports 页面

导航菜单更新（加入 3 个新入口）

loan_matcher_result.html 完整风控展示

加入 AI Risk Advisor Panel

所有页面可调用 FastAPI Phase 3–5 功能

不删除任何现有主要模块

✅ 请立即执行以上 Phase 6 全部任务。