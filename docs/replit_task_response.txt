给你一段"可直接粘贴给 Replit 的回复"，不用他们再问你。内容清晰、边界明确、带验收口径与回传格式。

⸻

▶️ 回复 Replit（原样发送即可）

标题： 启用 TASK_SECRET_TOKEN 并完成轮转/下载验收（只做设置与验证，禁止改主链路）

范围（不得越界）
	•	❌ 不修改：银行解析、7态状态机、UploadResponse、熔断、支持矩阵、页面文案。
	•	✅ 仅执行：保存文档 → 配置 TASK_SECRET_TOKEN → 重启 → 按步骤验证 → 回报证据。

⸻

1) 保存两份文档（存档）
	•	将我提供的两份文件保存到仓库：
	•	docs/replit_secrets_work_order.txt
	•	docs/audit_brief_template.pdf
	•	仅保存，不做任何功能改动。

回报： 提交记录链接或变更摘要（文件路径与提交号）。

⸻

2) 配置 Secrets（必须）
	1.	在 Replit Secrets 新增/确认：

	•	TASK_SECRET_TOKEN = sk_rotate_<随机安全串>（例如：sk_rotate_Jj8pZrQf7w2A）
	•	（如未设）EVIDENCE_ROTATION_DAYS=30，EVIDENCE_KEEP_MONTHLY=1

	2.	重启两个服务：Flask(5000) 与 FastAPI(8000)。

回报： Secrets 面板截图（遮住 value）+ "服务已重启"说明。

⸻

3) 权限联调（Admin 必须 200，非 Admin/缺 Token 必须 401/403）
	•	以 Admin：
	•	GET /admin/evidence → 200
	•	GET /downloads/evidence/list → 200 JSON
	•	GET /downloads/evidence/latest → 下载 ZIP（Content-Type: application/zip）
	•	以 非 Admin 或未登录 访问以上路由 → 401/403（拒绝）
	•	调用轮转（需 Token）：

curl -X POST "https://<YOUR_DOMAIN>/tasks/evidence/rotate" \
  -H "X-TASK-TOKEN: <你的 TOKEN>" \
  -H "Cookie: <Admin 会话 Cookie>"

预期返回：

{"success": true, "kept": [...], "deleted": [...]}


	•	不带 Token 或非 Admin 调用 → 401/403

回报：
	•	Admin 成功请求与非 Admin/缺 Token 失败请求，各 1 条证据（响应文本或截图）。
	•	轮转返回的 JSON（原样）。

⸻

4) ZIP 完整性校验（必须）
	1.	以 Admin 下载 /downloads/evidence/latest → 得到 ZIP
	2.	解压，对其中任一 JSON 做 SHA256，并与 ZIP 里的 manifest.json 对应条目比对：

	•	macOS/Linux: shasum -a 256 evidence_1_metrics_before.json
	•	Windows PowerShell: Get-FileHash evidence_1_metrics_before.json -Algorithm SHA256

回报： 终端计算结果与 manifest.json.sha256 一致的证据（文本或截图）。

⸻

5) 审计抽查 SQL（必须）

在数据库执行并回传结果（可打码）：

-- 最近 50 条证据相关操作
SELECT action, ip_address, user_agent, reason, created_at
FROM audit_logs
WHERE action IN ('evidence_download','evidence_delete','evidence_rotation')
ORDER BY created_at DESC
LIMIT 50;

-- 最近 5 次轮转详情
SELECT *
FROM audit_logs
WHERE action='evidence_rotation'
ORDER BY created_at DESC
LIMIT 5;

回报： 两条查询结果（文本或截图）。

⸻

6) DoD 勾选（逐项回答 YES/NO）
	1.	evidence_bundles/ 为私有目录（不经 /static 直达）
	2.	证据相关路由 Admin-only，且有审计记录
	3.	/admin/evidence 页面可用，EN/ZH 切换正确
	4.	POST /tasks/evidence/rotate 返回 kept/deleted
	5.	最新 ZIP 内任意文件 sha256 与 manifest.json 一致
	6.	audit_logs 存在 download/delete/rotation 三类记录
	7.	Secrets 生效（TASK_SECRET_TOKEN/EVIDENCE_ROTATION_DAYS/EVIDENCE_KEEP_MONTHLY）
	8.	非 Admin 或缺 Token 访问被 401/403 拒绝
	9.	三色规范与 i18n 抽查 3 处无硬编码
	10.	主链路（上传→详情→状态按钮）未受任何影响

任一项未达标：请写明root cause与修复方案（仅限本设置范围），严禁 未经确认修改主链路。

⸻

7) 回滚预案（如需）
	•	临时隐藏 /admin/evidence 菜单项（不删路由），保留 Admin 直达。
	•	保留全部历史证据包；必要时手动整体导出。
	•	回滚不影响银行解析/状态机/熔断/支持矩阵。

⸻

最终交付格式（请一次性回我）
A. 文档入库证明（路径与提交号）
B. Secrets 截图（打码）与"服务已重启"说明
C. Admin 成功 & 非 Admin/缺 Token 失败各 1 条请求证据
D. 轮转返回 JSON（原样）
E. ZIP 完整性校验证据（命令输出与 manifest 对比）
F. 两条审计 SQL 的查询结果
G. DoD 勾选表（10项 YES/NO）

⸻

注意
	•	禁止触碰主链路任何逻辑与样式。
	•	如遇 401/403/500，请在本工单内说明原因，不得擅自改代码绕过；先核对：Admin 登录、Token 携带、目录权限、审计装饰器。

⸻

需要英文版？这里是镜像（简）：

Title / Scope / Secrets / Protected Routes / Rotation cURL / ZIP integrity / Audit SQL / DoD / Rollback / Deliverables — 同上，英文内容我已在项目 docs 里提供 SECTION_9_FINAL_DELIVERY.md。

⸻

发这段给他们即可。等他们按此回包（A–G）后，我帮你对照 DoD 逐项验收。
